// The library's loader (installation docs, "What the script does"): it downloads only the
// fallbacks this browser lacks (commandfor, closedby, interestfor, anchor positioning) and the
// checkbox `indeterminate` attribute, so live examples behave as they do on a reader's page.
import "@chrissgon/perfectui";

export default defineNuxtPlugin(() => {});
