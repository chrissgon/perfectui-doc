#### Getting Started

# Installation

Install the package, then import the stylesheet.

```bash
npm i @chrissgon/perfectui
```

> [!WARNING]
> Perfect UI no longer exposes anything on `window`.

### What the script does

It loads fallbacks. See [Button](button.md#styles) and [the source](../src/index.ts).
